#pragma kernel NBody

struct Particle
{
	float3 pos;
	float3 vel;
};

RWStructuredBuffer<Particle> buffer;

[numthreads(64,1,1)]
void NBody (uint3 id : SV_DispatchThreadID)
{
	Particle a = buffer[id.x];
	a.pos += a.vel;

	buffer[id.x] = a;
}
